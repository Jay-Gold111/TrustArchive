{
  "name": "trustarchive-contracts",
  "private": true,
  "version": "0.1.0",
  "type": "commonjs",
  "scripts": {
    "node": "hardhat node",
    "compile": "hardhat compile",
    "deploy:localhost": "hardhat ignition deploy ignition/modules/Notary.js --network localhost --reset",
    "deploy:credentials:localhost": "hardhat ignition deploy ignition/modules/CredentialCenter.js --network localhost --reset",
    "deploy:all:localhost": "hardhat ignition deploy ignition/modules/All.js --network localhost --reset"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.15"
  }
}
